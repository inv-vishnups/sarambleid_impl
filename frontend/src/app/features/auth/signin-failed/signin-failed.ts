import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-signin-failed',
  imports: [RouterLink],
  templateUrl: './signin-failed.html',
  styleUrl: './signin-failed.css',
  standalone: true
})
export class SigninFailed {}
